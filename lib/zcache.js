/**

1) consistent hashing across n servers
2) able to create and recreate cache clients (while staying client agnostic)
3) able to mark cache servers as down
4) able to fall back to other cache servers
5) able to mark other cache servers as back up
6) able to warm a cache server over a given period of time

**/

